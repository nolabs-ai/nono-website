// Type declaration for side-effect CSS imports (e.g. `import "./globals.css"`).
// Required since TypeScript 6, which no longer allows side-effect imports of
// modules without type declarations. Next.js handles the actual CSS bundling.
declare module "*.css";
